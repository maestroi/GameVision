Capture a gomeboy save state into this directory as `bedroom.state` (web UI
"Save state", or copy a `.state` file) before running the bedroom scenario.

The state is only used to start the experiment. After load, the model still
sees pixels only.
